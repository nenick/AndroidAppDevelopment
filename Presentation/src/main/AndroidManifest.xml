<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.presentation">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-sdk android:targetSdkVersion="21" />

    <application
        android:allowBackup="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".page.start.MainActivity"
            android:label="@string/activity_title_main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".page.userlist.view.UserListActivity"
            android:label="@string/activity_title_user_list"></activity>

        <activity
            android:name=".page.userdetails.view.UserDetailsActivity"
            android:label="@string/activity_title_user_details"></activity>
    </application>

</manifest>
